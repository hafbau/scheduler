import { useState } from "react";

const useApplicationData = initial => {


  const state = () => {

  };

  const setDay = () => {

  };

  return {
    state,
    setDay,
    bookInterview,
    cancelInterview
  };
};

export default useApplicationData;
